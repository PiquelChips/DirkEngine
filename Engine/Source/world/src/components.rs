/// Holds the visual description of an entity.
#[derive(Debug, Clone)]
// TODO: actually renderable
pub struct Renderable;

#[derive(Debug, Clone)]
pub struct Position {
    pub x: f32,
    pub y: f32,
    pub z: f32,
}
