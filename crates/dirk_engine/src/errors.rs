//! This module contains all engine related errors.

/// All other errors that could be produced by the engine at runtime.
pub enum EngineError {}

/// Any error that could occur during build.
pub enum EngineBuildError {}
